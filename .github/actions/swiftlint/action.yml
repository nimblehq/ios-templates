name: SwiftLint
description: Run SwiftLint in Docker (strict mode).

runs:
  using: composite
  steps:
    - name: Run SwiftLint
      uses: docker://norionomura/swiftlint:0.63.2_swift-6.1.3
      with:
        args: swiftlint --strict
