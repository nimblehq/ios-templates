# Project Conventions

Project-specific overrides that take precedence over team conventions (Layer 4).
Add exceptions and project-specific rules here.

<!-- Example:
## API Naming
This project uses camelCase for JSON fields (client requirement)
despite Compass recommending snake_case.

## Error Format
All API errors follow RFC 7807 (Problem Details) format.
-->
